import Route from '@ember/routing/route';

export default Route.extend({
  // currentUser: Ember.inject.service(),
  // model() {
  //    return this.store.queryRecord('pids', {
  //      current: "true"
  //    });
  //  },


});
